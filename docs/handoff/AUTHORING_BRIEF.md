# Final handoff authoring contract (integration working brief)

User wants a complete, actionable development handoff for Devin, NOT product implementation by this agent. Korean documents; no sending to Devin unless explicitly requested. Author docs only in this package directory. Repo D:\플랫폼\exam_ai_spec\project, clean main 0c1fb5dc8223d52889ca676bed13ad2e7d93fa4a.

Evidence: D:\Codex\exam-ai-audit-20260919 contains REVIEW A01–A36, backend/frontend/export reports; real-sample/REAL_SAMPLE_REVIEW S01–S12; source_inventory.json; CODEX_SUBSCRIPTION_OPTION.md. Read relevant evidence, do not rerun product or original data.

## Common decisions

- USER CONFIRMED target: multiple academies service. Must use tenant-scoped documents/assets/jobs/brand/history, server-side authorization and role permissions, multi-worker concurrency/fairness/quotas, reliability and backup/restore. Existing Next/FastAPI retained unless evidence requires change. No Gemini use, no automatic paid API fallback, no sharing personal account/auth.json with hosted Devin/public server.
- USER CONFIRMED execution target: academies use WEB ONLY; platform centrally provides AI inference and HWP conversion. Codex subscription is DEVELOPMENT/QA ONLY. Production server OpenAI API inference uses separate authentication/budgets/availability checks, Gemini excluded default. No tenant agent install/no Codex or Hancom needed on academy PCs. Production storage/queues require documented new-dependency decisions/approval, not local SQLite design. Account capabilities, hosting vendor, exact API model, budget, commercial Hancom server license are deployment decisions/gates, not excuses to leave core specifications undefined.
- Development PC may use subscribed Codex for approved isolated QA fixtures; Devin develops with mocks/cache-only tests and centrally configured Windows worker integration tests. Production AI is NOT authenticated by personal CLI login. Lowcost models benchmarked before routing; CLI0.118.0/settings incompatibility affects DEV tools only, not a required academy setup step.
- Local execution still sends selected images to OpenAI. Prior Gemini explicit approval is neither granted nor needed for chosen no-Gemini target. Record source-data handling policy and user authorization for actual new provider execution. Do not claim inference executed on Codex yet.
- Latest source truth: originals printed exam; human HWP reference for artifact form, has errors and 232/372 equations10pt, objective endnotes lack solutions, Q13 diagram absent. Existing Golden has 7 wrong objective answers (2③4③8④9①10⑤14⑤20⑤); cached VERIFIED_FINAL is false assurance.
- Benchmark: source order _02/base/_04/_03/_01, 23 major /28 scored leaves /31 nodes (20MC+3essay parents+8children), total100, MCchoices100+Q4statements3. Counts are golden-sample specific, NOT all-exam schema constraints.
- Scope V1 COMPLETE: math exam restore→verified canonical document→exception review→controlled natural language edit/undo→editable equations/faithful figures→layout/answer spaces→correct age-appropriate answer+solution endnotes→HWPX/HWP/PDF proven same revision. Include document/job library/resume and simple brand/template. Similar-question generation optional V1.1 feature disabled until specified/tested. Question-bank marketplace/payment/multisubject outside V1.
- Preserve printed labels (MC1–20, 논술형1 etc), separate immutable IDs/position/display. No automatic rewording/correction in RESTORE; author changes are EDIT lineage. AI proposed changes NOT HUMAN_VERIFIED automatically. Source faithful restoration then deliberate edits tracked with baseline/provenance.
- Canonical structured content single mutation boundary, versions, optimistic concurrency, persistent history undo creates NEW revision, immutable originals/artifacts, atomic changes. Verification semantic content vs style invalidation dependency graph.
- Proposed layout: A4 default per existing spec; reference HWP B4 not silently cloned. Two columns each2 MC problems, column-major lefttop→leftbottom→righttop→rightbottom, pair row blocks(1,3)(2,4), max height align with padding. Long MC/essays preserve11pt and answer spaces, no loss/overflow; explicit fit algorithm and group splitting rules needed. Row alignment proposed <=0.5pt, font11pt±0.1pt. Keep proposals labelled.
- Actual HWP equation objects with math numbers italic(it), units roman,11pt incl inline and labels/notes. Native/vector faithful redraw, original relations/labels/line styles; images may be explicitly draft fallback but final redraw not asserted from crops. Schema-based safe drawing primitives, never execute arbitrary model SVG/scripts.
- Real endnote objects:23 major-linked notes grouping essay children OR28 leaf-linked notes, policy fixed per template; cover all28 leaves. Student paper with endnotes is explicit chosen complete package; no accidental inline answer reveal. Separate student-only/answer/teacher modes are deliberate options with leakage tests.
- Verification proof fail-closed: checks have NOT_RUN/RUNNING/PASSED/FAILED/UNAVAILABLE, issues persist OPEN/RESOLVED/WAIVED with critical waivers prohibit final. Content revision verification then draft/internal artifact generation then artifact proof then final eligibility; avoid circular requirement final before render. Per-format proof, HWP unavailable blocks HWP final, not automatically all independently verified formats. Product must NOT label generic all-format VERIFIED_FINAL if requested proof missing.
- Never promise arbitrary inputs zero mistakes. Release0 critical unresolved/missing/incorrect exposed final in finite held-out supported corpus; capability limits explicitly reject unsupported cases. Human active review goal measured, no invented achieved SLA/modelaccuracy.
- Job nonterminal lifecycle with durable checkpoint/retry/cancel, bounded queue; completion after save. Local unauthorized websites must not drive worker; origin+session token+loopback+path validation; no credentials in DB/logs/frontend. New dependency decisions documented and approval rules followed.

## Shared identifier scheme

Requirements parent assigns REQ-*; architecture can use component names and INV-*; tests use AT-* with requirement references once parent PRD ready. Existing audit IDs A01–A36/S01–S12 map to work packages WP00–WP11 (parent owner).

Planned package: START_HERE.md, 01_PRODUCT_REQUIREMENTS.md, 02_ARCHITECTURE_CONTRACTS.md (arch agent), 03_PAGE_UX_SPEC.md (UX agent), 04_ACCEPTANCE_TEST_PLAN.md (test agent), 05_DELIVERY_PLAN.md, DEVIN_START_PROMPT.md, TRACEABILITY.csv, decisions/assumptions, evidence references. Parent compiles source evidence subset/zip, checks links/IDs, independent final reviewer.

All files must state proposed design vs observed existing facts. Cite real repo relative path:line for baseline claims; use source evidence report if detailed. No invented numerical quality success or model performance. No implementation changes, no external inference.
